<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                             xmlns:app="http://schemas.android.com/apk/res-auto"
                                             android:layout_width="match_parent"
                                             android:layout_height="wrap_content">

    <ImageView
            android:id="@+id/nestedItemIv"
            android:layout_width="40dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_height="40dp"
            android:src="@mipmap/ic_launcher"
            android:contentDescription="@string/app_name"
            />

    <TextView
            android:id="@+id/nestedItemTv"
            android:text="abcde"
            android:textSize="16sp"
            android:layout_width="0dp"
            app:layout_constraintStart_toEndOf="@id/nestedItemIv"
            app:layout_constraintTop_toTopOf="@id/nestedItemIv"
            app:layout_constraintBottom_toBottomOf="@id/nestedItemIv"
            android:layout_height="wrap_content"/>
</androidx.constraintlayout.widget.ConstraintLayout>